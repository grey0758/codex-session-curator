# OpenCodex Operations

Last verified: 2026-05-18

- `hongkong002` is now reachable at `156.225.19.113`.
- `hongkong005` is now reachable at `103.73.161.157`.
- `cliproxy1.opencodex.uk` is a DNS-only direct A record to `103.73.161.157` for hongkong005 CLIProxy on port `8317`; the old `cliproxy1-cloudflared.service` on hongkong005 is disabled/inactive.
- The hongkong002 to hongkong005 CLIProxy path is excluded from Mihomo TUN transport by routing `103.73.161.157/32` out `ens17`; hongkong005 has the return route for `156.225.19.113/32` out `ens17`.
- Production OpenCodex site runs on `hongkong002` from `/home/grey/opencodex` via `opencodex.service`.
- On 2026-05-15, the Windows Codex install command generated by `/ops/install/` was fixed for newer npm and Scoop/fnm/MSI Node installs: prefer Node.js LTS, refresh npm global PATH entries, install/repair VC++ x64 runtime when winget is available, use `--include=optional` without deprecated `npm config set optional true`, install the Windows native Codex package when npm needs a fallback, resolve the real npm global root via `npm root -g`, skip API/interface verification, then directly launch the interactive Codex TUI. Native `codex.exe` path detection is now informational and no longer blocks launch when Scoop uses a non-APPDATA global prefix.
- On 2026-05-15, the Windows command was updated to print whether the current PowerShell is elevated admin mode or normal user mode, including the current user and `$HOME`, before installing Node/Codex. Admin mode is allowed, but the command warns that it writes to the current administrator user's npm/Codex profile and that normal users should run it in their own PowerShell.
- Verification: `npm run build` passed, `opencodex.service` restarted active, and `https://open-codex.com/ops/install/` plus `https://open-codex.com/ops/claude-install/` returned HTTP 200 on `156.225.19.113`.
